# NUMM Watchlist

Personal watchlist app for films, series, manga, games, books.

## Deploy on Vercel

1. Create account on vercel.com
2. Install Vercel CLI: `npm i -g vercel`
3. Run `vercel` in this folder
4. Done — live URL in 2 minutes

## Run locally

```bash
npm install
npm start
```
